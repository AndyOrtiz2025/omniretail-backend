package com.omniretail.backend.ecommerce.entity;

/** Estados canónicos del cliente, alineados con el contrato del storefront. */
public enum CustomerStatus {
    active,
    inactive,
    archived
}
